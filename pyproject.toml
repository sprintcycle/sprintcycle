[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sprintcycle"
version = "0.9.2"
description = "PRD驱动的自我进化敏捷开发框架"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "litellm>=1.0.0",
    "diskcache>=5.0.0",
    "tenacity>=8.0.0",
]

[project.optional-dependencies]
# SSE transport for remote agent access (uvicorn for HTTP server, starlette for ASGI)
mcp-sse = ["uvicorn>=0.23.0", "starlette>=0.27.0"]
# Development dependencies
dev = ["mypy>=1.0.0", "pytest>=7.0.0", "pytest-asyncio>=0.21.0"]

[project.scripts]
sprintcycle = "sprintcycle.cli:cli"

[tool.setuptools.packages.find]
include = ["sprintcycle*"]

[tool.mypy]
ignore_missing_imports = true
python_version = "3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "strict"

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]
