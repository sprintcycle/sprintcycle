# Production build: assets are served from the same origin as the FastAPI app.
# Override only if you deploy the SPA on a different host (advanced).
# VITE_* are compile-time; the dashboard typically uses relative /api only.
