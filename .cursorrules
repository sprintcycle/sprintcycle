# SprintCycle — legacy pointer / 遗留指针

Canonical architecture rules: `.cursor/rules/sprintcycle-architecture-orchestration.mdc`
Repository agent baseline: `AGENTS.md`

Do not duplicate workflow logic here. Use layer-specific rules and commands under `.cursor/rules/` and `.cursor/commands/`.

不要在本文重复工作流逻辑。请使用 `.cursor/rules/` 与 `.cursor/commands/` 中的分层规则与命令。
